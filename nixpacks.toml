[phases.setup]
nixPkgs = ['nodejs_20', 'php84', 'php84Extensions.intl', 'php84Extensions.zip', 'php84Extensions.pdo', 'php84Extensions.pdo_pgsql', 'php84Packages.composer']

[phases.install]
cmds = [
    'composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader',
    'npm ci',
    'npm run build'
]

[start]
cmd = 'php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT'
